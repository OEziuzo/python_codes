print(logo)
print("Welcome to the secret auction program.")
end_bidding = False
bidders_details = {}

def winner():
  #print(bidders_details)
  highest_bid = 0
  for key in bidders_details:
    #print(key)
    if bidders_details[key] > highest_bid:
      highest_bid = bidders_details[key]
      highest_bidder = key
  print(f"The winner is {highest_bidder} with a bid of {highest_bid}.")

while not end_bidding:
  username = input("What's your name?: ")
  bid = int(input("What's your bid?: $"))
  bidders_details[username] = bid
  more_bidders = input("Are there any other bidders?: Type 'yes' or 'no'.\n").lower()
  if more_bidders == "no":
    end_bidding = True
    winner()
  elif more_bidders == "yes":
    clear()
  